#pragma once

#include "is_same.h"

namespace estd {
	namespace detail {
		template <typename A, typename B, typename... Rest>
		struct is_unique {
			static constexpr auto value = 
		};
	}
}
